use std::time::{SystemTime, UNIX_EPOCH};
#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}


pub mod blockimplementation {
    pub struct Block{
        pub date: 
    }
}
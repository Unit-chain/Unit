cmake_minimum_required(VERSION 3.22)
project(3)

set(CMAKE_CXX_STANDARD 17)
set(Boost_LIBRARY_DIR /usr/include)
set(Boost_INCLUDE_DIR /usr/include)
set(Nlohman /usr/include/json-3.10.5/include/nlohmann)
#set(Pthread_LIBRARY_DIR /usr/include)
find_package(Boost COMPONENTS system filesystem REQUIRED)
include_directories(${Boost_INCLUDE_DIR})
include_directories(${Nlohamn})
link_directories(${Boost_LIBRARY_DIR})

add_executable(3 main.cpp)
target_link_libraries(3 ${Boost_LIBRARIES})